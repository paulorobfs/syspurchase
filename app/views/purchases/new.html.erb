<div class="d-flex align-items-center">
  <div class="p-2 w-50">
    <span class="title-pages">New Purchase</span>
  </div>
  <div class="d-flex w-50 flex-row-reverse">
    <%= link_to('Back', purchases_path, class: 'btn btn-secondary') %>
  </div>
</div>

<%= render 'form', purchase: @purchase %>