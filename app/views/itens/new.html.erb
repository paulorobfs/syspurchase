<div class="d-flex align-items-center">
  <div class="p-2 w-50">
    <span class="title-pages">New Iten</span>
  </div>
  <div class="d-flex w-50 flex-row-reverse">
    <%= link_to('Back', itens_path, class: 'btn btn-secondary') %>
  </div>
</div>

<%= render 'form', iten: @iten %>