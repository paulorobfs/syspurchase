<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                  <%= link_to 'Home', '/', class: 'nav-link' %>
                </li>
                <li class="nav-item">
                  <%= link_to 'Merchants', merchants_path, class: 'nav-link' %>
                </li>
                <li class="nav-item">
                  <%= link_to 'Itens', itens_path, class: 'nav-link' %>
                </li>
                <li class="nav-item">
                  <%= link_to 'Purchases', purchases_path, class: 'nav-link' %>
                </li>
                <% if can? :manage, :all %>
                  <li class="nav-item">
                    <%= link_to 'Upload Data', upload_index_path, class: 'nav-link' %>
                  </li>
                <% end %>
                <% if can? :manage, User %>
                  <li class="nav-item">
                    <%= link_to 'Users', users_path, class: 'nav-link' %>
                  </li>
                <% end %>
            </ul>
          <span class="pe-3">
            <%= image_tag(current_user.image, size: '30x30', class: 'rounded-circle', title: current_user.name) if current_user.image %>
          </span>
            <form class="d-flex">
                <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "btn btn-outline-success" %>
            </form>
        </div>
    </div>
</nav>